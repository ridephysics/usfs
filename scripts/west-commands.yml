# Keep the help strings in sync with the values in the .py files!
west-commands:
  - file: scripts/west_commands/usfs.py
    commands:
      - name: usfs
        class: USFS
        help: build usfs tools
